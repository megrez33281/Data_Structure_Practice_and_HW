解法：

     使用float型別的Stack解題
     由於stack只用來存放數字，不須擔心儲存運算子的問題
     讀取時，若讀到的是運算元，將其存入stack中
     若讀到的是運算子，則從stack中取出兩個運算元與該運算子進行運算(注意：stack中上方的運算元放在運算子右邊，下面一格之運算元放在運算子左邊)
     利用calculate函式讀入char型別之運算子並計算答案
     當讀取完所有字元後，印出pop(&stack)
     註：若要取小數點，以%.2f表示 (ex：printf("%.2f",pop(&stack));